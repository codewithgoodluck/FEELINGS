rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All authenticated users (anonymous or otherwise) can read and write.
    // The app uses Firebase anonymous auth — every visitor is automatically signed in.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
